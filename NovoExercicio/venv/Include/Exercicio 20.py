# 20 - Como utilizar o módulos e o comando import e from / import no import.
from math import sqrt
num = int(input(" Digite um número : "))
raiz = sqrt(num)
print(" A raiz de {:.2f} é igual a {}.".format(raiz,num))